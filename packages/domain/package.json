{
  "name": "@aircon/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Pure, deterministic domain logic: survey schema, heat-load estimation, pricing engine, confidence scoring. Zero runtime dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
