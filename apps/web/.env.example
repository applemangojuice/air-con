# Supabase project (Settings → API). Leave unset for demo mode:
# the quote flow still works end-to-end, but nothing is persisted.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
